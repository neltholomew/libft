#include "libft.h"

char *ft_substr(char const *s, unsigned int start, size_t len)
{
	(void)s;
	(void)start;
	(void)len;
	/* TODO: implement ft_substr - see the Field Guide for hints */
	return (NULL);
}
